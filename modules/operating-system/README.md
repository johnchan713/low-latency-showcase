# Operating-system modules

CPU affinity, scheduling, NUMA policy, huge-page control, and other explicit
operating-system integrations belong here.

Every module must isolate platform-specific code and document privileges,
kernel interfaces, failure behaviour, and safe fallbacks.
