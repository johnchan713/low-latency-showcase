# Memory modules

Allocators, object pools, data layouts, cache-line techniques, and memory
placement capabilities belong here.

Every module must document ownership, lifetime, alignment, allocation, NUMA,
and portability assumptions that affect correctness or latency.
