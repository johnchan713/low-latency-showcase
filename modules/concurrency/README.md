# Concurrency modules

Queues, synchronization strategies, wait policies, ownership transfer, and
other independently reusable concurrency building blocks belong here.

Every module must state its thread-safety, memory-ordering, progress, capacity,
and overload guarantees.
