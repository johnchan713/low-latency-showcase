# Repository information application

`lls_showcase` reports the project version, compiler, and active C++ language
mode. It also provides the repository smoke test through `--self-test`.

This application is deliberately small. Demonstrations of individual modules
belong beside those modules, while integrations of several modules belong in
the root `examples/` directory.
